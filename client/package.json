{
  "name": "client",
  "private": true,
  "scripts": {
    "dev": "pnpm -C .. exec vite -- --config vite.config.ts --host 0.0.0.0 --port 5173",
    "build": "pnpm -C .. exec vite -- build --config vite.config.ts",
    "check": "pnpm -C .. check"
  }
}
